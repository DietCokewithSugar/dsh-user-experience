# dsh-user-experience v0.1.1 修订文档

> 文档性质：个人开发执行清单
> 版本：v0.1.1（可用性修订）
> 前置：v0.1 已发布并实际使用
> 状态：待开发 · **优先级高于 v0.2**

---

## 1. 本次修订解决什么

v0.1 实跑后暴露两个问题，都不是功能缺失，而是形态错误。

**问题 A：报告卡片是技术文档，不是给人看的。**
卡片给出了文件路径和规则 ID，但没有一句话说清"在哪个页面、出了什么事、严不严重"。人被迫先读技术细节才能理解问题，而人在这个环节唯一要做的事只是判断"这算不算问题"。

**问题 B：确认流程是一串手工命令，不是流水线。**
`/ux judge <报告ID> <findingID> <confirmed|rejected>` 要求用户搬运系统内部主键。更根本的是，整套流程被设计成"每步必经人工关卡"——这是 Claude Code 式的形态，而 DSH 的价值主张是 agent 能在真实场景中持续自主工作。作者本人使用都不顺手，首次接触者只会更差。

**问题 B 的根因**：v0.1 把 R7（改动触发的自动走查）放进了 P1 没有实现。少了自动触发这一环，插件就只剩一串手动命令，形态自然退化成命令行工具。

---

## 2. 本次已定决策

| 议题 | 决定 |
|---|---|
| 默认运行模式 | **按场景自动选择**：交互中用 `review`，CI / headless 用 `auto` |
| 隐式确认 | **采纳**，实现跨走查指纹 |
| R7 优先级 | 从 P1 **提升为 P0**——它是自动化流水线的缺失环节 |

---

## 3. 变更一：报告卡片重构（双读者模型）

### 3.1 核心认识

**报告有两个读者，他们要的信息完全不同。**

| 读者 | 想知道 | 现状 |
|---|---|---|
| 人 | 哪个页面、出了什么事、严不严重、我要不要管 | ❌ 得先读技术细节才能理解 |
| AI | 精确位置、规则 ID、验证路径、怎么改 | ⚠️ 混在叙述里，不够结构化 |

v0.1 把两者混在一张卡片里，结果对人像技术文档，对 AI 又不够结构化。**折叠只是表现层的补丁，底层数据结构就该分开。**

### 3.2 UXFinding 结构调整

```yaml
id: UX-0007
fingerprint: <见第 6 节>

surface: 管理员页面                    # 新增：人话的位置名

human:                                # 新增：给人看的部分
  headline: 删除用户后没有任何提示
  description: |
    管理员点击删除后界面没有任何变化，无法判断操作是否成功，
    很可能重复点击导致误删多条记录。
  severity_label: 一级问题             # P0→一级，P1→二级，以此类推

technical:                            # 折叠区，可整块复制给 AI
  locator: { file: src/pages/Admin/UserTable.tsx, symbol: UserTable }
  rule: R-06
  verified_by: model+ast
  persona_refs: [admin-operator]
  severity: { impact: high, impact_confidence: high, reach: wide, level: P0 }

status: pending | confirmed | rejected
```

### 3.3 卡片默认呈现

```
┌────────────────────────────────────────────┐
│ [一级问题]  管理员页面                       │
│ 删除用户后没有任何提示                       │
│                                            │
│ 管理员点击删除后界面没有任何变化，无法判断    │
│ 操作是否成功，很可能重复点击导致误删多条记录。 │
│                                            │
│  [ 确认存在 ]  [ 不是问题 ]   ▸ 技术细节     │
└────────────────────────────────────────────┘
```

- 默认只展示 `surface` + `severity_label` + `headline` + `description` + 两个按钮
- 技术细节默认折叠，展开后提供**一键复制**，复制内容为结构化 YAML，直接可粘给 AI
- 严重度用人话（一级 / 二级 / 三级 / 四级问题），`P0`~`P3` 退为内部标识不再上界面

### 3.4 `surface` 字段的生成

这是本次新增的实现工作量。人话页面名的来源按优先级：

1. 路由配置中的 `title` / `meta.title`
2. 页面根组件内的 `h1` 文本
3. 面包屑或导航中指向该路由的链接文案
4. 以上都没有时，由模型根据组件名与页面内容拟一个中文名称

**兜底规则**：拟不出人话名称时用路由路径（`/admin/users`），但**不得**退化为文件路径——文件路径对人没有意义。

### 3.5 描述文案的写法约束

`human.description` 必须写"用户会遇到什么"，不写"代码里缺什么"。

| ❌ 不要 | ✅ 要 |
|---|---|
| `handleDelete` 的 catch 分支中没有调用 toast | 删除失败时界面没有任何提示，用户以为删成功了 |
| 缺少 empty state 分支 | 列表为空时页面一片空白，看不出是没数据还是加载失败 |

写进提示词，作为 finding 生成的硬性要求。

---

## 4. 变更二：运行模式与确认流程

### 4.1 三档模式

| 模式 | 行为 |
|---|---|
| `auto` | 全自动跑完直接出报告，不打断，不索要确认 |
| `review` | 出报告后一次性批量确认（可勾选多条一并提交） |
| `interactive` | 逐条确认（v0.1 现状，保留给需要精细调优规则的用户） |

### 4.2 场景自动选择规则

判定顺序，先命中先生效：

1. **命令行显式指定** `--mode` → 直接采用
2. **`.ux/rules.local.yml` 中的 `mode`** → 采用
3. **自动探测**：
   | 场景 | 模式 | 判定依据 |
   |---|---|---|
   | CI / headless 运行 | `auto` | `process.env.CI`、headless bundle、无 Web Client 连接 |
   | **由 R7 自动触发的走查** | `auto` | 触发来源是代码改动而非用户 |
   | 用户主动发起 `/ux` | `review` | 有交互界面且是人发起 |

**第 2 行是关键**：R7 发生在交互会话中，但它是 agent 主动发起的。如果这时弹出确认清单，等于在用户写代码写到一半时打断他——那正是 v0.1 被诟病的行为。**agent 自己发起的走查，就该由 agent 自己消化，安静出报告，只在出现一级问题时提示一句。**

### 4.3 确认操作不再需要 ID

会话中存在"当前报告"上下文，用户不需要知道任何 ID。三种入口：

- **卡片按钮**：点击直接生效
- **自然语言**：「第 2 条不成立」「这几条都对」「删除那条我确认」
- **批量操作**：「三级以下全部忽略」「全部确认」

`/ux judge <报告ID> <findingID> <状态>` 保留为脚本调用接口，但**不再出现在任何面向用户的提示、错误信息与文档中**。用户看到的错误提示应该引导他点按钮或直接说话，而不是背命令格式。

---

## 5. 变更三：R7 提升为 P0

自动走查是"流水线"与"命令行工具"的分界线，必须在本版落地。

- 监听文件编辑类工具的执行结果，收集变更文件
- 扫描单元是**被改动文件所属的完整组件 / 页面**，不是 diff 的那几行（缺失型问题在 diff 里不存在）
- 触发后以 `auto` 模式运行
- 只在出现一级 / 二级问题时向用户提示一句，其余静默入报告
- 可在 `.ux/rules.local.yml` 中关闭

---

## 6. 变更四：隐式确认与跨走查指纹

### 6.1 机制

**下一次走查时某条 finding 消失了 = 用户把它改掉了 = 这条问题成立。**

用户什么都不用点。而且这个信号比人工点"确认存在"更硬：点按钮只表示"我认为它成立"，改掉了表示"它真的值得改"。

### 6.2 指纹构成

跨走查比对需要一个在代码编辑后仍然稳定的标识。**不能用 `file:line`**——加一行代码位置就漂了。

```
fingerprint = hash(
  rule_id,                          # R-06
  symbol_path,                      # src/pages/Admin/UserTable.tsx::UserTable::handleDelete
  feature_digest                    # 文案类取文案 hash；结构类取被指认元素的语法特征
)
```

- `persona_refs` **不进指纹**——同一问题被不同 persona 命中时是合并的
- `surface` **不进指纹**——人话名称会变
- 重命名导致漂移的缓解：三元组中两项相同即视为同一问题，进入模糊匹配

### 6.3 必须区分「扫了没发现」与「根本没扫」

**这是整个机制最容易出错的地方。** finding 消失有三种可能：

1. 用户改掉了 → 隐式确认成立
2. 这次走查范围没覆盖到那里 → **不能判定**
3. 代码被整块删除 / 页面下线 → 不算"改进"，也不该计入指标

因此隐式确认**仅在同一 scope 被重新扫描时生效**。走查记录必须存下本次的 scope，比对时先判断该 finding 的位置是否落在本次 scope 内。

### 6.4 状态机扩展

```
pending              尚未判定
confirmed_explicit   用户点了"确认存在"
confirmed_implicit   下次走查中消失，且该位置确实被重新扫描
rejected             用户点了"不是问题"
stale                该位置本次未被扫描，无法判定
```

指标计算时 `confirmed_explicit` 与 `confirmed_implicit` 合并计入有效问题；`stale` 不计入分母。

---

## 7. 存储分层补充

v0.1 的 5.4 需要补一条。注意区分**判定结果**与**指标账本**——前者是阶段性的，后者是长期的。

| 数据 | 位置 | 提交 git | 说明 |
|---|---|---|---|
| 单次判定（confirmed / rejected） | 会话日志 | ❌ | 阶段性事实，维持 v0.1 决定不变 |
| **指纹历史账本** | `.ux/history.jsonl` | ❌ gitignore | 新增。记录指纹、首次出现、末次出现、终态、每次走查的 scope。这是长期指标数据，不是判定结果 |

`.gitignore` 追加 `.ux/history.jsonl`。

---

## 8. 验收标准

**卡片**
- Given 任意一条 finding，Then 卡片首屏不出现文件路径、规则 ID、`P0`~`P3` 等内部标识
- Given 某页面无法拟出人话名称，Then `surface` 退化为路由路径而非文件路径
- Given 用户展开技术细节，Then 可一键复制出结构化的完整技术信息

**运行模式**
- Given CI 环境，When 执行走查，Then 以 `auto` 运行且全程不索要确认
- Given R7 自动触发，Then 以 `auto` 运行，仅在出现一级 / 二级问题时提示一句
- Given 用户主动发起 `/ux`，Then 以 `review` 运行并在报告末尾提供批量确认

**确认**
- Given 报告已生成，When 用户说「第 2 条不成立」，Then 对应 finding 被标记为 rejected，全程无需 ID
- Given 任意面向用户的提示或错误信息，Then 其中不包含 `<报告ID>` `<findingID>` 之类的参数格式

**隐式确认**
- Given 某 finding 在下次走查中消失且其位置在本次 scope 内，Then 状态置为 `confirmed_implicit`
- Given 某 finding 的位置本次未被扫描，Then 状态置为 `stale` 且不计入指标分母
- Given 代码重命名导致位置变化，When 三元组中两项匹配，Then 仍视为同一 finding

---

## 9. 风险

| 风险 | 缓解 |
|---|---|
| 指纹漂移导致同一问题被反复当成新问题 | 三元组模糊匹配；上线后抽查重复率 |
| 隐式确认把"删代码"误判为"改进" | 6.3 的 scope 校验；必要时增加"该组件是否仍存在"的检查 |
| `auto` 成为默认后，人工确认数据大幅减少 | 正是隐式确认要补的缺口；同时报告卡片长期可点，异步确认随时可做 |
| 模型写不出人话 description | 提示词中给正反例（3.5 的对照表）；写不出人话的 finding 宁可不报 |

---

## 10. 阶段

| 阶段 | 内容 | 出口标准 |
|---|---|---|
| 1 | UXFinding 结构调整（surface / human / technical 三块） | 类型定稿 |
| 2 | 卡片重构 + 一键复制 + 人话严重度 | 首屏无内部标识 |
| 3 | 三档模式 + 场景自动选择 | CI 与交互下行为符合预期 |
| 4 | 确认入口去 ID 化（按钮 / 自然语言 / 批量） | 提示与文档中不再出现 ID 参数 |
| 5 | R7 自动触发落地 | 改动后自动出报告且不打断 |
| 6 | 指纹与隐式确认 | 状态机五态可用，指标可算 |

**v0.1.1 完成前不启动 v0.2。** 形态问题不修，渲染能力做得再好也是在错的骨架上加功能。
