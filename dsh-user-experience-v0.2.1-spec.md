# dsh-user-experience v0.2.1 问题卡片话术层说明

> 文档性质：问题卡片（spec §R4 确认闭环）话术层的执行说明，是 [v0.1 spec](dsh-user-experience-v0.1-spec.md) §R4 / §R5 的增量补充。
> 版本：v0.2.1（卡片话术层：人话在前、技术细节折叠）
> 状态：已完成

---

## 1. 背景

R4 的确认闭环由**人**来做判定，但 v0.2 的卡片写给编译器看：一行 `P0 R-04` 徽标 + 文件路径 + AST 断言原文。要确认「这算不算问题」，读者得先读懂规则编号和判定依据——把技术验证过程当成了结论本身。

反馈明确了分工：**人只需要知道"在哪儿、发生了什么"，然后确认这算不算问题；错误成因不必读**。技术细节仍然要留着，但它的读者是 AI——确认成立之后，整段交给模型去改。

于是卡片改成两屏：

| 屏 | 内容 | 读者 | 默认状态 |
|---|---|---|---|
| 第一屏 | 场景（在哪儿）、一句话摘要（发生了什么）、影响（后果）、影响用户、中文严重度、确认按钮 | 人（产品 / 运营 / 开发） | 展开 |
| 第二屏 | 文件位置、规则、证据等级、判定依据、修复方向、「复制给 AI」 | AI（人不必逐字读） | 折叠 |

---

## 2. 数据模型改动

`UxFinding` 增加**人话层**三字段（`src/types.ts`），与既有技术层字段并存：

| 字段 | 必填 | 含义 | 反例 |
|---|---|---|---|
| `scene` | 是 | 问题所在的场景/页面，用户对得上号的说法：「管理员页面 · 用户列表」 | 文件路径、组件名 |
| `summary` | 是 | 一句话说明发生了什么：「删除用户没有二次确认，点一下就直接删掉了」 | 出现文件名 / 规则 ID / 代码术语 |
| `consequence` | 否 | 对用户造成的后果：「运营批量处理时容易误删，删掉的账号找不回来」 | 复述判定依据 |

`ux/report` 会话事件增加 `personas?: Array<{ id, name }>`：卡片说「影响用户：后台运营」而不是 `ops`。事件仍以 id 为准，名称只是展示用快照。

严重度不新增字段——`P0…P3` 由 impact × reach 矩阵推导（spec §5.3）不变，只是展示时翻译为「一级问题…四级问题」并附一句解释（`src/human.ts`）。

---

## 3. 话术层实现（src/human.ts）

Host（Markdown 报告）与 Client（卡片）共用同一个模块，保证两处话术一致；模块只依赖规则目录与类型，不碰 node 内建模块，可被 client bundle 内联。

- `severityWording(level)`：P0 → 一级问题 +「大多数目标用户会被挡住，关键任务做不完」；
- `categoryWording` / `ruleWording`：`state-coverage` → 状态覆盖、`R-04` → `R-04 不可逆操作缺二次确认`；
- `sceneFallback(file, symbol)`：symbol → 文件名 → `index.*` 取目录名；
- `summaryFallback(rule, suggestion)`：规则名 + 修复方向；
- `handoffText` / `handoffBundle`：「复制给 AI」的文本——人话结论在前，定位与技术细节在后，整份复制时带一句交付指令并逐条编号。

---

## 4. 模型侧约束

`ux_report` 的 `scene` / `summary` 声明为 required：工具层直接校验，模型漏填会拿到 `INVALID_ARGS` 并按提示补写，卡片第一屏因此不会退化成技术文档。三处提示词同步约束了写法（不得出现文件名 / 规则 ID / 代码术语）：

- `ux_report` 工具描述与参数描述；
- `ux_scan` 的 guidance；
- `/ux scan` 的流程提示与 R2 注入的走查协议（`renderPersonaGuidance`）。

---

## 5. 兼容性（老事件重放）

确认状态是持久化会话状态，加人话层之前写入的 `ux/report` 事件必须照样能重放：

- 缺 `scene` / `summary`：客户端组装视图时落到 `sceneFallback` / `summaryFallback`；
- 缺 `personas`：画像展示回退到 id；
- 空白字符串（模型敷衍）与 Host 侧同路径处理：`trim()` 后为空即走兜底；
- 同一位置同一规则合并时，人话层**首条为准、只补空**（后续画像的 `consequence` 可以补上首条缺的那条，不覆盖已有表述）。

---

## 6. 报告与卡片输出

Markdown（`ux_report`）每条问题的结构：

```text
- **【一级问题】管理员页面 · 用户列表** — 删除用户没有二次确认，点一下就直接删掉了
  - 影响：运营批量处理时手一抖就误删，删掉的账号找不回来
  - 涉及用户：后台运营、个人投资者（大多数目标用户会被挡住，关键任务做不完）
  - 技术细节 · UX-0001：`src/pages/Admin/UserList.tsx:42（UserList）`｜规则 R-04 …｜证据 static / model+ast
    - 判定依据：…
    - 修复方向：…
```

卡片（`src/client/report-view.ts`）：严重度徽标显示中文等级；确认按钮改为「确认是问题 / 不是问题」，判定后内联显示「✓ 已确认是问题 / ✗ 已标记为不是问题」；折叠开关带 `aria-expanded`；复制走 Clipboard API，非安全上下文回落到临时 textarea + `execCommand`，两条路都不通才提示手动复制（复制是辅助动作，不阻断确认）。整份复制排除已判定「不是问题」的条目。

展开/复制属于瞬时视图状态，留在组件内；判定状态仍然只从会话事件来，不引入本地状态。

---

## 7. 测试覆盖

- `scripts/smoke.ts`：严重度/分类/规则中文化、`scene` 三级兜底（symbol → 文件名 → index 取目录名）、`summary` 兜底、`handoffText` / `handoffBundle` 结构；
- `scripts/e2e.ts`：
  - 人话层随 finding 落定与留空兜底、合并补空 `consequence`；
  - Markdown 以「【一级问题】场景」开头、技术细节缩进为子条目、汇总用中文等级计数；
  - `ux/report` 事件携带画像名称快照；
  - 卡片视图数据的人话字段与技术字段；
  - 老事件（无 `personas` / 无人话层）重放兜底；
  - `react-dom/server` 静态渲染卡片：中文等级、场景与摘要、影响用户、确认按钮、技术细节默认折叠（`aria-expanded="false"`）、整份复制入口。

---

## 8. 未做

- 判定后的「改主意」入口：判定即终态，按钮消失（事件模型支持覆盖，UI 未开）；
- 卡片内直接触发修复（把技术细节交给 AI 目前靠复制，不自动发起会话）；
- 话术的多语言：当前话术层是中文常量，英文界面需要先引入 locale 命名空间。
