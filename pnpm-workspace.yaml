allowBuilds:
  esbuild: true
# 官方 DSH 框架包（0.1.0-rc.x，developer preview）发布频繁，且本仓库对所有
# @deepseek-ai 依赖精确锁版（无 ^/~ 范围解析，见 package.json），上游版本变化
# 不会自动进入构建——按 pnpm 官方建议对其豁免 24h 最小发布年龄冷却；
# 其余第三方依赖保持 pnpm 默认供应链策略（minimumReleaseAge: 1440 分钟）。
minimumReleaseAgeExclude:
  - '@deepseek-ai/*'
