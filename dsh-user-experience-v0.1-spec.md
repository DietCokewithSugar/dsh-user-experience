# dsh-user-experience v0.1 需求与开发执行文档

> 文档性质：个人开发执行清单
> 项目类型：DeepSeek Harness（DSH）开源插件，MIT
> 版本：v0.1（源码输入）
> 状态：待开发

---

## 1. 问题陈述

现有的自动化界面检查工具（axe、Lighthouse）只能校验绝对规则——对比度够不够、有没有 alt 属性。但体验问题的本质是**相对的**：删除前的二次确认弹窗，对偶尔操作的普通用户是必要保护，对每天处理上百条记录的后台操作员就是纯粹的效率损耗。同一个设计，换个用户，结论相反。脱离了"给谁用"，"体验问题"这个概念无法定义，这是现有工具的能力天花板。

与此同时，体验问题被发现的时机普遍太晚——通常要等到可交互原型或上线后的用户反馈，此时返工成本最高。而在需求和开发阶段，代码里其实已经藏着大量可判断的信号：错误提示写了什么、异步失败后用户看到什么、列表为空时渲染什么。这些信号今天没有任何工具在读。

另一个空白在 code review 阶段。现在的 review agent 已经能自动发现 bug、安全问题和代码规范问题，但**没有任何一个能指出"这次改动引入了体验问题"**——比如新增的删除操作没有二次确认、新写的异步请求失败后用户看不到任何提示。改动进入 review 时是最后一个低成本拦截点，过了这道关就要等到线上反馈。

DSH 插件生态目前清一色是开发者自用工具（UI 皮肤、记忆、上下文压缩、插件管理），**垂直领域能力为零**，没有任何一个插件从体验视角切入。

---

## 2. 目标

1. **让"目标用户"成为走查的前置输入**——所有问题判定都必须挂靠到一个明确的用户画像上，无 persona 不出结论。
2. **在开发阶段就产出可行动的体验提示**——不追求穷尽和精确，追求"提醒开发者去看一眼"的早期信号价值。
3. **每条问题可定位、可复核**——用户能一眼找到位置并判断是否成立，判断结果被记录下来。
4. **沉淀两个可复用的数据结构**（Persona / UXFinding），使后续的网站、设计图输入能作为 provider 接入，而不是重写项目。

### 不追求的指标（反向指标）

**"发现的问题总数"明确不作为优化目标。** 越优化它，插件越倾向于凑数、把主观偏好包装成问题。这个数字越高越危险。

---

## 3. 非目标（v0.1 明确不做）

| 非目标 | 原因 |
|---|---|
| 网站 / 在线站点输入 | 需要无头浏览器 provider 与渲染链路，留给 v0.2 |
| UI 设计图输入 | 依赖 grounding 能力与外部视觉插件接口，留给 v0.3 |
| 需求文档（PRD）输入 | 本质是评审而非走查，产出难验证，且目标用户是手里有仓库的开发者 |
| 模型调用成本优化 | v0.1 先验证方向与价值。方向没验证之前做成本优化，会为了省 token 而限制发现力，本末倒置 |
| React + TypeScript 以外的技术栈（Vue、小程序等） | 单一技术栈才能把这批规则做准，扩展留待规则质量验证之后 |
| 视觉类问题判定（对比度、热区尺寸、文字截断、焦点顺序） | 纯静态代码无法确证，强行判定会产生大量误报 |
| 自动修复 / 自动改代码 | 体验改动涉及产品决策，不应由插件单方面执行 |
| 自建 agent 循环 | DSH 已提供 `/loop` 与 `ctx.goals`，重写等于对抗框架 |
| 自建浏览器驱动、视觉模型适配器 | 生态已有成熟实现，直接复用 |
| star / 装机量类传播指标 | v0.1 自用阶段收集不到，也不应影响开发决策 |

---

## 4. 用户故事

**主要用户：正在开发项目的开发者 / 设计师（v0.1 即本人）**

1. 作为开发者，我希望插件先和我确认这个产品是给谁用的，这样它给出的体验问题才是针对我的用户，而不是泛泛而谈。
2. 作为开发者，我希望 AI 能先从代码和 README 里推一版用户画像草稿给我改，这样我不用从零开始写。
3. 作为开发者，我希望画像能存进仓库并提交到 git，这样团队共享同一份共识，也不用每次会话重问一遍。
4. 作为开发者，我希望每条问题都告诉我它在哪个文件或哪个组件，这样我能立刻去看一眼判断真假。
5. 作为开发者，我希望能对每条问题点"成立"或"不成立"，这样报告能收敛成一份我真正认可的清单。
6. 作为开发者，我希望问题按严重度排序，这样我能先处理阻断性的那几条。
7. 作为开发者，我希望每条问题告诉我往哪个方向改，而不是直接改我的代码。

**边界场景**

8. 作为开发者，当项目是 monorepo 或包含多个应用时，我希望插件问我走查哪一个，而不是把所有代码混在一起分析。
9. 作为开发者，当我的项目技术栈不在支持范围内时，我希望插件明确告诉我不支持，而不是给出一堆低质量猜测。

---

## 5. 核心数据结构

**这是本项目的核心资产。规则会过时、会被抄，一套被接受的问题描述结构不会。先定义结构，再写检测逻辑。**

### 5.1 Persona

存储位置：`.ux/personas.yml`（仓库内一等公民文件，可 git 提交、可 review、可 diff）

```yaml
personas:
  - id: novice-investor
    name: 首次使用的个人投资者
    scenario: 下班后用手机快速查看持仓变化
    goals: [快速确认盈亏, 判断要不要操作]
    capability:
      tech_literacy: low          # low | medium | high
      device: mobile              # mobile | desktop | both
      network: unstable           # stable | unstable
      accessibility_needs: []     # 如 low_vision / motor
    key_paths: [登录, 查看持仓, 下单]
    share: 0.7                    # 占目标用户比例估计，用于计算影响范围
```

**约束：AI 推断的画像只能作为草稿，必须经用户确认或修改后才写入文件并生效。**
锚点优先级：用户填写 > README / 落地页 / 产品文案 > 从代码结构推断。
理由：若画像完全由代码推出，它必然与代码现状自洽，走查只能查出细节不一致，查不出根本问题。

### 5.2 UXFinding

```yaml
id: UX-0007
persona_refs: [novice-investor]   # 命中该问题的 persona 列表；长度 >1 即为共性问题
category: microcopy | state-coverage
severity:
  impact: high | low              # 是否阻断该 persona 完成关键任务
  reach: wide | narrow            # 受影响用户占目标用户比例，以 persona.share 估算
  level: P0 | P1 | P2 | P3        # 由上面两轴推导
evidence:
  level: static                   # v0.1 固定为 static
  verified_by: model+ast          # model | model+ast | ast，用于归因无效问题来自哪条路径
  locator:
    file: src/pages/Portfolio.tsx
    symbol: PortfolioList         # 组件级定位即可，行号可选
rationale: 规则 ID R-03 / 或启发式条目原文
suggestion: 优化方向描述（不给具体代码）
status: pending | confirmed | rejected
```

**硬约束：没有 locator 的问题不输出。** 查得出但指不到位置的结论，验证成本高于价值。

### 5.3 严重度矩阵

| | reach: wide（影响多数目标用户） | reach: narrow（仅影响少数） |
|---|---|---|
| **impact: high**（阻断关键任务） | **P0** 必须改 | **P1** 应该改 |
| **impact: low**（可完成但体验受损） | **P2** 建议改 | **P3** 可选 |

**reach 的定义卡死为"受影响用户占目标用户的比例"，不是受影响页面的数量。**

多 persona 场景下 reach **由命中该问题的 persona 的 `share` 之和推导，不再人工估计**——这也是 `persona.share` 字段存在的意义。

### 5.4 数据存储分层

按"是阶段性事实还是长期偏好"划分，不要混在一起。

| 数据 | 存储位置 | 是否提交仓库 | 理由 |
|---|---|---|---|
| 单次问题的成立 / 不成立判定 | **会话日志**（`SessionEventMap`） | 否 | 这是阶段性事实——问题改掉就不存在了。写进仓库文件会让它每次都被 review，制造无意义的审查负担 |
| Persona 画像 | `.ux/personas.yml` | **是** | 项目级共识，团队共享，且 CI 模式依赖它 |
| 术语表与判定 | `.ux/glossary.yml` | **是** | 项目级事实，复用价值高 |
| 走查偏好规则 | `.ux/rules.local.yml`（gitignore） | 否 | 个人偏好，不该强加给团队 |

**走查偏好规则**用于避免用户重复表达同一件事：

- 关闭某条规则（"这个项目不需要查二次确认"）
- 指定重点关注方向（"重点看错误提示和空态"）
- 排除特定目录或文件

配置一次长期生效。**用户表达过的意愿不该在下一次走查中被再问一遍，这是最容易让人烦的地方。**

---

## 6. 需求

### P0 — 不做就不成立

**R1. Persona 初始化**
- 提供人工命令（注册到 `ctx.commands`，不消耗模型轮次）触发
- 路径一：AI 读取 README / package.json / 路由结构，生成 **1–3 个**画像草稿，逐个交用户确认是否有误
- 路径二：用户跳过草稿直接填写，插件引导补全必填字段
- 用户可修改、增删，确认后写入 `.ux/personas.yml`
- 已存在该文件时直接加载，**不重复询问**（同一业务的目标用户通常不变，重复填写是最容易劝退用户的地方）

验收：
- Given 项目根目录无 `.ux/personas.yml`，When 用户触发初始化命令，Then 输出 1–3 个画像草稿并等待用户确认
- Given 用户修改了草稿并确认，Then 文件被写入且内容与用户确认的一致
- Given 文件已存在，When 用户发起走查，Then 直接加载不再询问

**R2. Persona 注入上下文**
- 通过 `ctx.systemPrompt.section()` 注入当前生效画像（对齐官方 AGENTS.md 的 section provider 模式）

验收：走查产出的每条 finding 都带有非空的 `persona_refs`，且其中每个 ID 都存在于文件中

**R3. 源码走查工具 `ux_scan`**
- 注册到 `ctx.tools`
- 技术栈范围：**仅 React + TypeScript**。检出非支持技术栈时明确告知不支持，不给出低质量猜测
- v0.1 只实现下列 9 条高置信度静态规则
- 实现方式：**模型判断为主，AST 求证为辅**（详见附录 A.3）

**微文案类**
| ID | 规则 | 检测信号 |
|---|---|---|
| R-01 | 错误提示无行动指引 | catch / error 分支的用户可见文案只描述失败，无下一步建议 |
| R-02 | 术语不一致 | 同一概念在不同页面使用不同词（如"账户/帐号/用户"混用） |
| R-03 | 不可逆操作文案泛化 | delete / remove / clear 类操作使用"确定""提交"等无信息量文案 |
| R-04 | 不可逆操作缺二次确认 | 删除 / 清空类调用前无确认交互 |

> **R-02 优先级最低，条件触发**：仅当本轮未发现 P0 / P1 问题时才执行，属于"没有更要紧的事可报"时的补充检查。
> **结果必须持久化**：提取出的术语表与判定写入 `.ux/glossary.yml`，后续走查直接复用，只对新增或变更的术语做增量判断，不重复全量比对。跨文件比对是这批规则里最贵的一条，每次重跑纯属浪费。

**状态覆盖类**
| ID | 规则 | 检测信号 |
|---|---|---|
| R-05 | 有 loading 无 empty | 列表渲染存在加载分支但无空数组分支 |
| R-06 | 有 success 无 error | 异步调用无 catch，或 catch 内无用户可见反馈 |
| R-07 | 提交中按钮未禁用 | 异步提交流程中无 pending 态锁定 |
| R-08 | 无超长内容兜底 | 直接渲染用户输入字段，无截断或占位处理 |

**主题适配类**
| ID | 规则 | 检测信号 |
|---|---|---|
| R-09 | 深色 / 浅色模式适配缺失 | 组件内出现硬编码颜色字面量（hex / rgb）而未走主题变量；或 Tailwind 类名写死 `text-*` / `bg-*` 而无对应 `dark:` 变体 |

> R-09 说明：这是本组置信度最高的规则——信号完全静态可判、几乎不产生误报，且属于高频真实问题（背景切到深色但文字颜色未跟随）。与其他规则不同，它不依赖 persona，任何用户都会受影响。

验收：
- Given 一个含已知问题的测试项目，When 执行 `ux_scan`，Then 每条命中的 finding 都包含 category、severity、locator、rationale、suggestion
- Given 某问题无法定位到文件或组件，Then 该问题不进入输出

**R4. 问题确认闭环**
- 在 Web Client 中以卡片渲染每条 finding，提供"成立 / 不成立"操作
- 实现方式：注册 `ConversationNodeDefinition` + `conversation.chat.node` keyed renderer
- 确认结果作为持久化会话状态（扩展 `SessionEventMap`），支持重放

验收：
- Given 报告已生成，When 用户标记某条为 rejected，Then 该状态写入会话日志
- Given 会话重新加载，Then 此前的确认状态完整恢复

**R5. 报告输出**
- 输出 Markdown 报告，按 P0→P3 排序
- 仅统计 confirmed 的问题进入最终汇总
- 报告中标注证据等级为 `static`，并声明本版不覆盖视觉类问题

**多 persona 走查与合并**

存在多个 persona 时，**先逐个 persona 独立走查，再合并成一份综合报告**。合并后的报告分两部分：

| 部分 | 内容 | 作用 |
|---|---|---|
| **共性问题** | 被 2 个及以上 persona 命中的问题（`persona_refs` 长度 >1） | 多视角独立命中同一处，可信度显著更高，排在报告最前 |
| **各 persona 个性问题** | 仅被单一 persona 命中的问题，归在该 persona 名下 | 保留视角差异，不因为"只有一个画像在意"就丢弃 |

- 合并时按 locator 去重：同一位置同一规则视为同一问题，合并其 `persona_refs`
- 共性问题的 reach 自然更高（share 之和更大），严重度会随之上升，无需额外规则

验收：
- Given 项目有 2 个以上 persona，When 执行走查，Then 报告包含"共性问题"与"各 persona 个性问题"两部分
- Given 同一 locator 同一规则被多个 persona 命中，Then 合并为一条 finding 且 `persona_refs` 包含全部命中者
- Given 某问题仅被 1 个 persona 命中，Then 该问题保留在对应 persona 的个性问题中，不被丢弃

**R6. 走查范围确定（执行顺序在 R3 之前）**

大仓库不做全量扫描。范围收敛按以下顺序：

1. 优先查找项目内的架构说明文件（`ARCHITECTURE.md`、`AGENTS.md`、README 的结构章节、docs 目录），据此定位模块边界与入口
2. 若不存在此类说明，**主动询问用户本次想走查哪个功能 / 页面 / 业务流程**，并引导其补充更具体的描述（如"下单流程从选品到支付成功"）
3. 用户描述模糊时继续追问，不在范围未明的情况下开始扫描

验收：
- Given 项目存在架构说明文件，When 发起走查，Then 插件据此提出范围建议并交用户确认
- Given 项目无架构说明文件，When 发起走查，Then 插件询问具体功能 / 页面 / 业务流程，不直接全量扫描
- Given 用户描述过于笼统（如"看看首页"），Then 插件继续引导补充，直至范围可界定

### P1 — 显著改善但不阻塞发布

- 走查偏好规则文件 `.ux/rules.local.yml`：按 ID 关闭规则、指定重点方向、排除目录（见 5.4）
- 重复否决时提示固化：同一条规则被连续否决多次后，询问用户是否写入偏好文件永久关闭
- monorepo 多应用选择
- 确认 / 否决数据跨会话累积统计，用于计算指标

**R7. 会话内增量走查（改动触发）**

在 DSH 会话中，当 agent 或用户修改了文件后，自动对改动涉及的范围执行一次走查，无需手动发起。

- 实现方式：监听 `tools/result` 观察文件编辑类工具的最终结果，收集变更文件；空闲时通过 `agent.inject()` 送入提示，或按 `/loop` 模式在 `turn/end` 后 `followup()`
- **扫描单元是"被改动文件所属的完整组件 / 页面"，不是 diff 的那几行**。理由见下方约束
- 仅提示 P0 / P1，P2 以下不打断当前工作流
- 可通过配置关闭

**改动走查的关注面（R7 与 P2 的 CI 模式共用）**

| 优先级 | 范围 | 说明 |
|---|---|---|
| 优先 | 与本次改动相关的**业务流程**的体验 | 不是孤立看被改的文件，而是看这次改动所处的业务链路 |
| 重点 | **前端改动** | 界面与交互的变化用户直接可感知，是体验问题的主要来源 |
| 默认排除 | 纯后端改动 | 用户不可感知，报出来只是噪音 |
| **例外纳入** | 影响用户等待时间的改动 | 性能属于体验：接口响应变慢、首屏加载变长、同步阻塞替代异步，即使改在后端也应报出 |

**关键约束：diff 看不见"缺失"。**
体验问题大量是缺失型的——没有空态、没有错误分支、没有二次确认。这些在 diff 里是不存在的行，扫 diff 必然漏掉。所以增量走查只用 diff 来**确定范围**，判断仍在完整组件上做。这一点决定了实现方式，不能省。

### P2 — 现在不做，但架构上要留口

- `UXSource` capability seam：Service Definition + Provider + Consumer 三件套完整设计，为 v0.2 网站输入、v0.3 设计图输入预留（官方明确：只有一个角色不构成 seam）
- 视觉能力声明为**可选依赖**：用户配置了多模态模型或安装了视觉插件则升级为 `rendered` 证据，否则自动降级到 `static` 并如实标注——**缺视觉不报错退出**
- 证据等级枚举预留 `rendered` / `heuristic`
- **性能类规则**：等待时间是体验的一部分（阻塞式请求、首屏串行加载、大列表无虚拟化）。v0.1 不做规则实现，但改动走查已把它列为后端改动的例外纳入项
- 设计图输入路线已定：由多模态模型**直接读图**走查，不经"图转代码"中转；grounding 仅用于问题定位
- **CI / PR 阶段走查**：以 headless 模式（`dsh-headless`，一次性运行、无服务端）对 `git diff` 范围执行走查，结果作为 PR 评论输出
  - 前置条件天然满足：`.ux/personas.yml` 已经是仓库内的提交文件，CI 环境无需交互即可拿到 persona
  - 输出必须按严重度过滤，只报 P0 / P1——PR 里的噪音机器人会被直接静音或移除，这是这条路线唯一的失败模式
  - v0.1 需要保证的架构口子：`ux_scan` 的输入必须能接受一个显式的文件范围列表，而不是只能自己决定扫哪里

---

## 7. DSH 扩展点映射

| 模块 | 官方机制 | 备注 |
|---|---|---|
| Persona 初始化命令 | `ctx.commands` | 人工命令，不走模型轮次 |
| Persona 注入 | `ctx.systemPrompt.section()` | 对齐 AGENTS.md 模式 |
| 走查工具 | `ctx.tools.register()` | schema 自动进入提示词组装 |
| 逐项走查循环 | `/loop`（turn/end → followup）或 `ctx.goals` | **不自建循环** |
| 问题卡片与确认 | `ConversationNodeDefinition` + keyed renderer | Web Client 业务节点 |
| 确认状态持久化 | 扩展 `SessionEventMap` | 从日志渲染与重放 |
| 输入源适配（v0.2+） | capability seam：定义 + provider + consumer | |
| 模型无关 | 无需开发 | 官方模型配置已支持自定义 OpenAI 兼容端点 |

**红线：任何设计都不得修改 agent-loop。** 官方架构的核心主张是"没有一行产品功能修改主循环"，若方案需要改核心，说明形态设计错了。

---

## 8. 成功指标

v0.1 只看两条先行指标，跑完一轮真实项目即可测量。

**指标一：有效问题占比 ≥ 50%**
定义：报告中被标记为 confirmed 的问题数 ÷ 问题总数。
含义：一半以上的提示对使用者有价值即算过关。
说明：本插件定位是开发阶段的**早期提示**，不是判决书。过分追求准确率会导致插件不敢报，把真正有价值的早期信号一起过滤掉。

**指标二：定位有效性 ≥ 90%**
定义：locator 能准确指向问题所在组件或页面的比例。
粒度：**组件 / 页面级即可，不强求 file:line。**
说明：这一条比指标一严格。问题报错了使用者可以自行判断真假，但指不到位置的问题连判断的机会都没有，成本直接转嫁给使用者。**准确率可以松，定位不能松。**

**测量方式**：每次走查后手动完成确认闭环，插件自行记账。

---

## 9. 开放问题

| 问题 | 需要谁决定 | 是否阻塞 |
|---|---|---|
| （当前无未决问题，新增问题在此登记） | — | — |

---

## 10. 阶段与节奏

| 阶段 | 内容 | 出口标准 |
|---|---|---|
| 0. 结构先行 | 定义 Persona 与 UXFinding schema，写成 TypeScript 类型 | 两个结构定稿，不再频繁改动 |
| 1. 骨架 | 插件加载、命令注册、persona 初始化与文件读写 | `/ux init` 可跑通并生成文件 |
| 2. 检测 | 9 条规则 + `ux_scan` 工具 + Markdown 报告 | 在 1 个真实项目上产出完整报告 |
| 3. 闭环 | 问题卡片、确认 / 否决、状态持久化 | 确认状态可重放 |
| 4. 校准 | 在 3 个自有项目上实跑，测量两条指标 | 两条指标达标；未达标则回到阶段 2 收紧规则 |

**规则宁缺毋滥：9 条不达标就砍到 5 条，不要为了覆盖面凑数。**

---

## 11. 发布检查项（v0.1 完成后）

- [ ] 仓库添加 `dsh-plugin` topic（官方发现机制）
- [ ] 向 awesome-dsh-plugin 提 PR，各语言 README 各加一行
- [ ] README 安全提示：GitHub 来源插件在安装时会于用户机器执行构建脚本，提示只装可信来源并锁定 commit（`github:owner/repo#sha`）
- [ ] README 明确声明 v0.1 的能力边界：仅源码、仅静态证据、不覆盖视觉类问题
- [ ] 锁定所依赖的 DSH 版本（框架处于 developer preview，接口会变）

---

## 附录 A：AST 解析是什么，以及规则该怎么分工

### A.1 AST 是什么

AST = 抽象语法树（Abstract Syntax Tree）。把代码从"一段文本"解析成一棵结构化的树，每个节点是一个语法概念：函数声明、JSX 元素、if 分支、属性、字符串字面量。

例如：

```jsx
{isLoading ? <Spinner /> : <List items={data} />}
```

解析后大致是：

```
条件表达式
├─ 条件：标识符 isLoading
├─ 为真：JSX元素 Spinner
└─ 为假：JSX元素 List
         └─ 属性 items = 标识符 data
```

**为什么不用正则或字符串搜索**：搜 `text-black` 会把注释里的、字符串常量里的、变量名里的一起搜出来。AST 知道它到底是不是一个 JSX 元素 className 属性里的类名——结构清楚，不误伤。

对本项目两条规则的具体用法：

- **R-05（有 loading 无 empty）**：找到组件返回的 JSX，检查条件渲染分支——存在 `isLoading` 分支，但不存在"数组为空"的分支
- **R-09（深色模式适配）**：遍历 JSX 属性节点，找 className 中的 `text-*` / `bg-*` 类，检查是否有对应的 `dark:` 前缀类；或找 style 对象中的 hex 字面量

工具链（React + TypeScript 生态成熟）：TypeScript 官方 compiler API、`@babel/parser`，或封装更友好的 `ts-morph`（建议从这个入手）。

### A.2 两条路径的能力边界

| | AST 解析 | 交给模型判断 |
|---|---|---|
| 能查什么 | **结构上的有无**：分支缺失、属性缺失、调用缺失 | **语义质量**：这句话写得好不好、这两个词是不是一个意思 |
| 成本 | 零 token，毫秒级 | 按代码量消耗 token |
| 稳定性 | 同样输入必定同样输出 | 每次结果可能不同 |

### A.3 分工：混合方案（已定）

**模型判断为主，AST 作为具体验证。**

```
模型扫读范围内代码 → 提出候选问题 → AST 对可结构化验证的部分求证 → 通过则出 finding，不通过则丢弃
```

这个顺序对应两条成功指标的分工：

- **模型负责召回**——它能读懂语义，也能发现规则清单之外的问题，服务于"早期提示"的定位
- **AST 负责精度**——把模型的猜测降为可证伪的结构断言，直接压低无效问题占比

| 规则 | AST 可验证什么 |
|---|---|
| R-04 不可逆操作缺二次确认 | 该调用路径上确实不存在确认交互节点 |
| R-05 有 loading 无 empty | 条件渲染分支中确实不存在空数组分支 |
| R-06 有 success 无 error | 该异步调用确实无 catch，或 catch 内无渲染输出 |
| R-07 提交中按钮未禁用 | 提交流程中确实无 pending 态绑定到 disabled |
| R-08 无超长内容兜底 | 该字段确实被直接渲染，无截断或占位处理 |
| R-09 深色模式适配缺失 | 该 className 确实无对应 `dark:` 变体；该颜色确实是硬编码字面量 |
| R-01 错误提示无行动指引 | 仅能验证"这是错误分支中的用户可见文案"，文案质量本身无法验证 |
| R-02 术语不一致 | 仅能提取候选术语位置，是否同义由模型判断 |

**快车道例外**：R-09 是纯结构问题，可跳过模型直接由 AST 出结论，零 token。R-04、R-05、R-07 同理，视实测效果决定是否也走快车道。

**记录验证来源**：finding 中的 `verified_by` 字段标明该问题是否经过 AST 求证。这是后续判断"无效问题主要来自哪条路径"的唯一依据——若未验证项的无效率显著更高，则应扩大 AST 求证覆盖面。
